import { Directive, HostBinding, HostListener, Input, OnInit, ElementRef } from '@angular/core';

@Directive({
  selector: '[PasswordValidation]'
})
export class PasswordValidationDirective  {
 // @HostBinding('pattern1') pattern1: string;
//   @Input() pattern: string = "^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9]).{8,}$";
  
//   constructor(private pattern1: string){}

//   




}